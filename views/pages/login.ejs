<!DOCTYPE html>
<html lang="en">

<head>
  <title><%= typeof locals.title != undefined || locals.title !== ""  ? locals.title : "Movie Watchlist" %></title>
  <% include ../layouts/header %>
</head>

<body>
  <% include ../layouts/navbar %>

  <div class="wrapper fadeInDown container mt-5 p-5">
    <div id="formContent">
      <form method="POST" action="/login">
        <div class="form-group">
          <input type="text" id="username" class="form-control fadeIn second" name="username" placeholder="username">
        </div>
        <div class="form-group">
          <input type="password" id="password" class="form-control  fadeIn third" name="password" placeholder="password">
        </div>
        <div class="form-group">
          <div class="text-danger text-bold">
            <%= typeof locals.message != undefined || locals.message != "" ? locals.message : "" %>
          </div>
        </div>
        <div class="form-group">
          <input type="submit" class="form-control btn btn-primary fadeIn fourth" value="Log In">
        </div>
      </form>
    </div>
  </div>

  <br></br>
  <br></br>
  </div>
  </div>
  </div>
</body>

</html>